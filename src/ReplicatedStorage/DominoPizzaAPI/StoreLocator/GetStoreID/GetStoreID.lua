local GetStoreId = {}

local HttpService = game:GetService("HttpService")

function GetStoreId.GetStoreId() -- return an array of all the store id's (key : store location, value : store ID) 
    NearByStore = require(game.ReplicatedStorage.DominoPizza)
    
end

return GetStoreId
