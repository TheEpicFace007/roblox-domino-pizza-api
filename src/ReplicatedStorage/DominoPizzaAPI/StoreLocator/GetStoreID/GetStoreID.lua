local GetStoreId = {}

local HttpService = game:GetService("HttpService")

function GetStoreId.GetStoreId() -- return an array of all the store ids
    NearByStore = require(game.ReplicatedStorage.DominoPizza)
    
end

return GetStoreId